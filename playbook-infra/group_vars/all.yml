# ========================================
# Global Ansible Configuration
# ========================================

# Credenciales MikroTik (usar ansible-vault en producci�n)
ansible_connection: network_cli
ansible_network_os: community.routeros.routeros
ansible_user: admin
ansible_password: admin
ansible_port: 22
ansible_command_timeout: 60

# Variables globales de red
core_subnet: "10.10.0.0/16"
loopback_subnet: "10.10.255.0/24"
management_subnet: "10.10.0.0/16"

# Configuraci�n NTP (opcional)
ntp_servers:
  - "10.10.10.9"

# SNMP Configuration
snmp_community: "public"
snmp_version: "2c"
