---
- name: Ensure base packages are present
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - gnupg
      - lsb-release
      - python3-apt
    state: present
    update_cache: true

- name: Set timezone
  ansible.builtin.timezone:
    name: "{{ platform_timezone }}"
